import 'package:flutter/material.dart';

class ManagesScreen extends StatelessWidget {
  const ManagesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ManagesScreen'),
      ),
    );
  }
}
